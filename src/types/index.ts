export type Medicine = {
    id: string
    name: string
    image_url?: string
    purchase_date: string
    expiry_date: string
    description?: string
    dosage?: string
    ingredients?: string
    user_id: string
  }